export class PythonDto {
  // public sessionId: string;
  // public cssBackgroundTemplate: string;
  // public cssTextTemplate: string;
  // public red: number;
  // public green: number;
  // public blue: number;


  constructor(public sessionId: string, timestamp: number, cssBackgroundTemplate: string, cssTextTemplate: string, red: number, green: number, blue: number) {
    // this.sessionId = sessionId;
    // this.cssBackgroundTemplate = cssBackgroundTemplate;
    // this.cssTextTemplate = cssTextTemplate;
    // this.red = Math.trunc(red);
    // this.green = Math.trunc(green);
    // this.blue = Math.trunc(blue);
  }
}
